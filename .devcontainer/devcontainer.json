{
	"name": "ferronn-dev/addonmaker",
	"build": {
		"context": "..",
		"dockerfile": "Dockerfile"
	},
	"features": {
		"docker-from-docker": "latest"
	},
	"postCreateCommand": "pre-commit install && pre-commit run -a",
	"remoteUser": "vscode"
}
